name = "nexusgraph-api"
main = "src/index.ts"
compatibility_date = "2024-08-01"
compatibility_flags = ["nodejs_compat"]

[vars]
PORT = "8787"

# Secrets to set using `npx wrangler secret put <NAME>`:
# - SUPABASE_URL
# - SUPABASE_SERVICE_ROLE_KEY
# - GITHUB_TOKEN (optional)
